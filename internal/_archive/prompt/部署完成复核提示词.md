# 生产部署复核提示词 — byocc.cc

> 工作目录：D:\code\build-your-own-claude-code  
> 当前目标域名：`https://byocc.cc`

请对当前 `feat/multi-file-edit-system` 分支上的**部署准备改动**做一次专门的 deployment review，重点看“开发可跑”是否真的转成了“部署可跑”。

## 本轮部署相关改动

### 1. Lab 文件配置生成

- 新增 [server/scripts/generate-lab-config.mjs](D:/code/build-your-own-claude-code/server/scripts/generate-lab-config.mjs)
- 新增 [server/src/services/lab-files-generated.ts](D:/code/build-your-own-claude-code/server/src/services/lab-files-generated.ts)
- 修改 [server/src/services/lab-workspace.ts](D:/code/build-your-own-claude-code/server/src/services/lab-workspace.ts)
- 修改 [server/package.json](D:/code/build-your-own-claude-code/server/package.json)

当前策略：
- `platform/src/lib/lab-files.json` 是唯一 source of truth
- server build 前运行 generator
- generator 生成 `server/src/services/lab-files-generated.ts`
- 运行时 server 直接 import 生成的 TS 常量，不再跨目录 `readFileSync platform/src/...`

### 2. 生产环境变量模板

- 新增 [server/.env.example](D:/code/build-your-own-claude-code/server/.env.example)

### 3. 前端 API 默认同源

- 修改 [platform/src/lib/api.ts](D:/code/build-your-own-claude-code/platform/src/lib/api.ts)

当前策略：
- `NEXT_PUBLIC_API_URL` 默认留空
- 浏览器默认走 `/api/*`
- 依赖 Next.js rewrite 转发到 `127.0.0.1:3001`

### 4. 终端 WebSocket 生产化

- 修改 [server/src/routes/environment.ts](D:/code/build-your-own-claude-code/server/src/routes/environment.ts)
- 修改 [platform/src/components/Terminal.tsx](D:/code/build-your-own-claude-code/platform/src/components/Terminal.tsx)

当前策略：
- server 返回相对路径 `/api/terminal/:sessionId?token=...`
- 前端 Terminal 组件收到相对路径后，按当前页面 origin 自动转成 `ws://` / `wss://`
- 这样浏览器只和 `byocc.cc` 通信，不直连 localhost 或容器端口

### 5. build.mjs 同步

- 已把本地 `.tmp/lab-image-context/runtime/build.mjs` 同步到 sister repo：
  - `D:\test-claude-code\claude-code\build.mjs`

当前状态：
- sister repo 工作树里 `build.mjs` 已修改，但未在本仓库 git 中追踪

### 6. 启动脚本

- 新增 [start-production.ps1](D:/code/build-your-own-claude-code/start-production.ps1)

### 7. Cloudflare Tunnel 路由模板

- 新增 [infrastructure/cloudflared.config.example.yml](D:/code/build-your-own-claude-code/infrastructure/cloudflared.config.example.yml)

当前策略：
- `/api/terminal/*` 通过 Cloudflare Tunnel 直接转发到 Express (`localhost:3001`)
- 其他 `byocc.cc` 流量走 Next.js (`localhost:3000`)
- 避免 WebSocket upgrade 被 Next.js rewrite 吃掉

## 请重点 review

### A. 生产路径与构建
- generator 路径是否稳健
- `server build` 是否一定会先生成 `lab-files-generated.ts`
- 生成文件是否应该 commit，还是只在构建时生成
- dev / build / start 三条路径是否一致

### B. 前端 API 与 rewrite
- `platform/src/lib/api.ts` 默认 `API_BASE=""` 是否正确
- 会不会有某些调用绕过 rewrite 直接访问后端
- `NEXT_PUBLIC_API_URL` 在生产应为空这一点是否已经通过代码层面保证

### C. WebSocket 生产路径
- `environment.ts` 返回相对路径是否合理
- `Terminal.tsx` 的 `resolveWebSocketUrl()` 是否能正确构造 `wss://byocc.cc/...`
- `cloudflared.config.example.yml` 里的 `/api/terminal/* -> 3001` 是否能避免 WebSocket upgrade 丢在 Next.js 3000

### D. build.mjs 同步策略
- sister repo 的 `build.mjs` 同步是否到位
- 当前仓库是否还需要额外记录“如何把 build.mjs 改动同步到镜像”

### E. 安全配置
- `server/.env.example` 是否覆盖了上线所需关键项
- 是否还缺少 byocc.cc 部署时必须设置的变量说明
- `server/.env.example` 里是否足够明确 `node --env-file` 的 Node 版本门槛

## 已完成验证

```bash
cd server && npx tsc --noEmit --project tsconfig.json
cd server && npm run build
cd platform && npx tsc --noEmit --project tsconfig.json
cd platform && npm run build
cd server && npm test
git diff --check
node --check D:\test-claude-code\claude-code\build.mjs
```

## 输出格式

请按以下格式输出：

```text
[CRITICAL]

[HIGH]

[MEDIUM]

[LOW]

[POSITIVE]

[VERDICT: APPROVE / REQUEST_CHANGES]
```
